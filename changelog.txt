VERSION[0.10.2][2026/06/06]

--- ツリー表示時の CPU 過負荷を削減し、0_4 ツリーのクラッシュを修正 / Reduce passive-tree CPU load and fix a 0_4-tree crash ---
* パッシブツリー表示時（特に「Show Node Power」／ヒートマップ）の CPU 過負荷とフリーズを解消。macOS では sg.GetTime() が秒を返すのに上流 src/ の全消費側はミリ秒前提で書かれており、(1) アイドル検出が秒スケールのため常にスロットルされず約 49% の CPU を消費、(2) CalcsTab のノードパワー再計算の協調的 yield ゲート（GetTime() - start > 100 が 100 秒の意味になる）が約 2 秒の全ツリー走査中に一度も yield せず 1 フレームでフリーズ、という 2 症状が同一原因から発生していた。GUI 境界で `_G.GetTime` を ms 正規化（×1000）する 1 箇所のラップで全消費側を一括修正（pob2_launch.lua）
* 0_4（以前）のパッシブツリーで「PassiveTreeView.lua:664: attempt to index field 'handle' (a nil value)」クラッシュを修正。これらのツリーは .dds.zst テクスチャ配列を使い macOS ポートではロードできず、ロード失敗側の分岐で handle=nil を格納してアセット不変条件（全レコードが非 nil の画像ハンドルを持つ）を破っていた。失敗時も LoadImage が常に生成する空ハンドル（data.handle）を格納するよう修正し、ロード時に nil ハンドルがあれば error する不変条件チェックを追加（src/Classes/PassiveTree.lua）
* ※Lua のみの変更。zip の再ダウンロード（再インストール）が必要です

* Fixed excessive CPU use and freezes while the passive tree is shown (especially "Show Node Power" / the heat map). On macOS sg.GetTime() returns seconds, but every upstream src/ consumer assumes milliseconds, so two symptoms shared one root cause: (1) idle detection on a seconds scale never throttled and burned ~49% CPU, and (2) the CalcsTab node-power recalc's cooperative yield gate (GetTime() - start > 100 became 100 seconds) never yielded during the ~2-second full-tree walk and froze a whole frame. Fixed all consumers with a single ms-normalizing wrap of `_G.GetTime` (×1000) at the GUI boundary (pob2_launch.lua).
* Fixed a "PassiveTreeView.lua:664: attempt to index field 'handle' (a nil value)" crash on 0_4 (and older) passive trees. Those trees use .dds.zst texture arrays the macOS port cannot load, and the failed-load branch stored handle=nil, breaking the asset invariant that every record carries a non-nil image handle. It now stores the empty handle LoadImage always creates (data.handle) even on failure, plus a load-time invariant check that errors if any record has a nil handle (src/Classes/PassiveTree.lua).
* Note: Lua-only change; requires re-downloading the zip (reinstall).

VERSION[0.10.1][2026/06/06]

--- 上流 PoB2 バグ修正 2 件を同期（Hollow Form／Deflect 上限）/ Sync 2 upstream PoB2 bugfixes (Hollow Form / Deflect cap) ---
* Hollow Form サポート（Mantra of Illusions）のステータスマッピングを上流修正に追随。トリガースキルの「攻撃速度+%（最終）」と「ダメージ+%（最終）」の MORE mod が入れ違っていたのを修正（src/Data/Skills/other.lua）
* Deflect 確率が上限（DeflectionChanceCap）でクランプされず上限を超過し得た計算を上流修正に追随（src/Modules/CalcDefence.lua）
* ※上流 dev 2df5a7433 まで同期（Release 0.18.0 + bugfix 3 件のうち、macOS フォークに該当する 2 件を適用）
* ※Lua のみの変更。zip の再ダウンロード（再インストール）が必要です

* Followed the upstream fix for the Hollow Form support (Mantra of Illusions) stat mapping: the triggered skill's "attack speed +% (final)" and "damage +% (final)" MORE mods were swapped (src/Data/Skills/other.lua).
* Followed the upstream fix for Deflect chance not being clamped to the cap (DeflectionChanceCap), which could let it exceed the cap (src/Modules/CalcDefence.lua).
* Note: synced up to upstream dev 2df5a7433 (Release 0.18.0 + 3 bugfixes; applied the 2 relevant to the macOS fork).
* Note: Lua-only change; requires re-downloading the zip (reinstall).

VERSION[0.10.0][2026/06/05]

--- 上流 PoB2 0.18.0 を同期（ゲームデータ・計算の最新化）/ Sync upstream PoB2 0.18.0 (latest game data & calculations) ---
* 上流 PathOfBuilding-PoE2 0.17.1 → 0.18.0 を取り込み（59 ファイル）。計算エンジン（CalcOffence／CalcDefence／CalcPerform／ModParser ほか 12 Modules）、ゲームデータ（ジェム／ミニオン／スペクター／ベース／ModCache／WorldAreas ほか 20 Data）、UI タブ（ConfigTab／ItemsTab／ImportTab／SkillsTab ほか 12 Classes）、新アイテムヘッダー画像 15 枚（ルーンアイテム枠）を更新
* ミニオンのクリティカル率計算を上流修正に追随
* アイテムのアーマー／回避／エネルギーシールド／ワードのツールチップ値を、上流の新 API（レベル依存計算）に追随しつつ日本語ラベルを保全
* ※トレード系の大規模改修（TradeQuery 一式）は次バージョンへ持ち越し（既知ギャップ）
* ※zip の再ダウンロード（再インストール）が必要です

* Synced upstream PathOfBuilding-PoE2 0.17.1 → 0.18.0 (59 files): the calculation engine (CalcOffence/CalcDefence/CalcPerform/ModParser and 12 Modules in total), game data (gems, minions, spectres, bases, ModCache, WorldAreas and 20 Data files), UI tabs (ConfigTab/ItemsTab/ImportTab/SkillsTab and 12 Classes), and 15 new item-header images (runic item frames).
* Followed the upstream fix for minion critical strike chance.
* Updated item Armour/Evasion/Energy Shield/Ward tooltip values to the upstream level-aware API while preserving Japanese labels.
* Note: the large trade-subsystem rework (the TradeQuery set) is deferred to the next release (a known gap).
* Note: requires re-downloading the zip (reinstall).

VERSION[0.9.4][2026/06/05]

--- スキルパネル操作時のログ氾濫と長時間フリーズを修正 / Fix log flooding and a long freeze around the skill panel ---
* 「五万行のエラー」の正体だった、毎フレームのデバッグ print 氾濫を停止。描画ループが `GetScreenSize: physical=...`（ノーガード、1 セッションで約 97 万行）と `Frame N: ...` 系（約 38 万行）を毎フレーム出力し、ランチャの `>> "$LOG"` でログが 87MB／143 万行に肥大していた。全ての per-frame print を環境変数フラグ `POB_DEBUG_FRAME`（デフォルト off、診断時のみ `=1` で再有効化）の背後に gate（pob2_launch.lua）。OnFrame の pcall エラー出力など正当なログは維持
* 約 67 秒のフリーズを修正。`main:WrapString` の CJK 分岐は 1 行を `DrawStringWidth` で繰り返し測りながら折り返すため文字列長 L に対し O(L²) で、数十万バイトの異常文字列が渡ると秒オーダーで固まっていた。WrapString 先頭で 10000 バイト超の文字列を 2000 バイトに clamp し、レンダラ到達前に長さを断つガードを追加（src/Modules/Main.lua）
* PoE2 に存在しない「Find Timeless Jewel」ボタンを非表示化。「Show Node Power」チェックボックスと重なって入力を奪っていた問題を解消（src/Classes/TreeTab.lua）
* ※Lua のみの変更。zip の再ダウンロード（再インストール）が必要です

* Stopped the per-frame debug-print flood behind the user-reported "50,000-line error". The render loop printed `GetScreenSize: physical=...` (ungated, ~974K lines per session) and `Frame N: ...` variants (~378K lines) every frame, bloating the log to 87MB / 1.43M lines via the launcher's `>> "$LOG"`. All per-frame prints are now gated behind the `POB_DEBUG_FRAME` env flag (off by default; set `=1` to re-enable for diagnostics) in pob2_launch.lua. Legitimate logging such as OnFrame pcall errors is preserved.
* Fixed a ~67-second freeze. `main:WrapString`'s CJK branch re-measures each line via `DrawStringWidth` while wrapping — O(L²) in line length L — so a malformed multi-hundred-KB string froze the app for seconds. Added a guard at the top of WrapString that clamps any >10000-byte string to 2000 bytes before it reaches the renderer (src/Modules/Main.lua).
* Hid the "Find Timeless Jewel" button, which does not exist in PoE2 and overlapped the "Show Node Power" checkbox, stealing its input (src/Classes/TreeTab.lua).
* Note: Lua-only change, but requires re-downloading the zip (reinstall).

VERSION[0.9.3][2026/06/05]

--- LaunchSubScript クラッシュ修正（オンライン機能の復旧）/ Fix LaunchSubScript crash (restores online features) ---
* OnFrame 中に `bad argument #2 to 'LaunchSubScript' (cannot convert 'string' to 'void *')` で落ちる問題を修正。macOS の dylib バインディングが 2 引数スタブ（scriptName, luaState）のままで、PoB 本来の多引数呼び出しと型が合わず FFI チェックで即クラッシュしていた（stage4 以来の潜在バグ。サブスクリプトを使う全機能＝ビルドのインポート/エクスポート、Pastebin/pobb.in/Maxroll/poe.ninja ダウンロード、PoB Archives 類似ビルド、ツリー PoEURL 解決が起動直後やそれら操作時に落ちていた）
* dylib は再ビルド不可のため、`_G.LaunchSubScript` を pure-Lua の同期シムに差し替え（pob2_launch.lua）。スクリプトをメイン Lua state で同期実行し、結果を毎フレーム OnFrame の `DrainSubScripts()` で遅延配送（コールバック登録が呼び出し後に行われる Pattern 2 にも対応）。9 箇所の呼び出し全てを単一の choke point でカバー
* 起動時/12時間ごと/Ctrl-U の自動アップデート確認（CheckForUpdate）が同じ経路でクラッシュしていたため no-op 化。macOS ポートはアプリ内自己アップデート非対応のため実害なし
* curl 経路に接続/全体タイムアウト（CONNECTTIMEOUT 10s / TIMEOUT 30s）を追加し、ダウンロード hang を防止（Launch.lua, BuildSiteTools.lua, PoBArchivesProvider.lua, TreeTab.lua）
* 注意: ダウンロードは現状メイン state で同期実行のため、取得中は最大タイムアウト分 UI が一時停止します
* ※Lua のみの変更。zip の再ダウンロード（再インストール）が必要です

* Fixed a crash in OnFrame: `bad argument #2 to 'LaunchSubScript' (cannot convert 'string' to 'void *')`. The macOS dylib binding was still a 2-arg stub (scriptName, luaState), which mismatched PoB's multi-arg calling convention and crashed at the FFI type check (a latent bug since stage4 — every sub-script feature crashed: build import/export, Pastebin/pobb.in/Maxroll/poe.ninja downloads, PoB Archives similar builds, and tree PoEURL resolution).
* Since the dylib cannot be rebuilt, `_G.LaunchSubScript` is replaced with a pure-Lua synchronous shim (pob2_launch.lua). Scripts run synchronously in the main Lua state and results are delivered deferred via `DrainSubScripts()` each frame in OnFrame (also handles Pattern 2 callers that register their callback after the call returns). A single choke point covers all 9 call sites.
* The auto update-check (CheckForUpdate, fired at startup / every 12h / Ctrl-U) crashed via the same path, so it is now a no-op. Harmless on the macOS port, which does not support in-app self-update.
* Added connect/total timeouts (CONNECTTIMEOUT 10s / TIMEOUT 30s) to the curl paths to prevent download hangs (Launch.lua, BuildSiteTools.lua, PoBArchivesProvider.lua, TreeTab.lua).
* Note: downloads currently run synchronously in the main state, so the UI pauses for up to the timeout while fetching.
* Note: Lua-only change, but requires re-downloading the zip (reinstall).

VERSION[0.9.2][2026/06/04]

--- Metal autoreleasepool リーク修正（ツリー操作の枯渇クラッシュ解消）/ Fix Metal autoreleasepool leak (passive-tree memory-exhaustion crash) ---
* パッシブツリー操作中にメモリが急増し数分でクラッシュする問題を修正。SimpleGraphic.dylib の Metal 描画ループに per-frame autoreleasepool が無く、autorelease された Metal オブジェクト（command encoder/buffer/drawable）が毎フレーム蓄積していた（実測: ツリー操作 10 秒で +500MB → 8GB 枯渇で AMD Metal ドライバが SIGBUS）
* dylib は再ビルド不可のため、glfwPollEvents（毎フレーム先頭で呼ばれる）を interpose する注入 dylib（libpob_poolfix.dylib）を DYLD_INSERT_LIBRARIES でロードし、フレーム先頭の安全な点で pool を排出（runtime/libpob_poolfix.dylib, package/poolfix/pob_poolfix.c）
* 検証: 同一ツリー操作で MALLOC_NANO が未修正 1.0G→1.5G に対し、修正版は 512M→512M でフラット・クラッシュ無し
* ※ランチャ＋新 dylib を含むため zip の再ダウンロード（再インストール）が必要。アプリ内アップデートでは反映されません

* Fixed a crash where memory ballooned and the app died within minutes while manipulating the passive tree. SimpleGraphic.dylib's Metal render loop had no per-frame autoreleasepool, so autoreleased Metal objects (command encoder/buffer/drawable) accumulated every frame (~500MB in 10s of tree manipulation → 8GB exhaustion → AMD Metal driver SIGBUS).
* Since the dylib cannot be rebuilt, a small injected dylib (libpob_poolfix.dylib) interposes glfwPollEvents (called at the top of each frame) and drains the autorelease pool at that safe point, loaded via DYLD_INSERT_LIBRARIES.
* Verified: under identical tree manipulation MALLOC_NANO stays flat (512M→512M) vs unfixed (1.0G→1.5G), no crash.
* Note: requires re-downloading the zip (reinstall); not deliverable via in-app update (it touches the launcher and adds a dylib).

VERSION[0.9.1][2026/06/03]

--- 0.5 スキル日本語翻訳 + Trade nil-safety / 0.5 skill JP translations + trade nil-safety ---
* PoE2 0.5 (Return of the Ancients) 新スキル 93 件の日本語訳を追加（ja.lua、PoE2DB JP 参照）
* Trade セッション cookie (POESESSID) を空文字デフォルト化し、未設定時の nil 連結クラッシュを防止（Main.lua）

* Added Japanese translations for 93 new PoE2 0.5 (Return of the Ancients) skills (ja.lua)
* Default the trade session cookie (POESESSID) to "" to avoid nil-concat crashes before it is set (Main.lua)

VERSION[0.9.0][2026/06/03]

--- 上流 0.17.1 同期（Phase 10）/ upstream 0.17.1 sync (Phase 10) ---
* 上流 PathOfBuilding-PoE2 dev を Release 0.16.0→0.17.1 まで同期（23 commits）
* 計算/データ層 229 ファイル: Zarokh's Revolt 無限DPS・Aggravated bleeding・Amazon クリティカル・Trarthan Cannon クラッシュ・Rapid Casting III・0.5 新データ/runes/quest rewards 等
* 0.17 のツリーアート（.dds.zst テクスチャ配列 BC7/BC1）を 2D アトラス PNG へ変換するパイプラインを構築（dds_to_png.py / convert_tree_dds.py）。macOS dylib は stb_image=PNG のみのため、配列レイヤをデコード→アトラス再パック→spriteCoords 化
* GUI 実機テストで tree 描画クラッシュ（ddsCoords 裸キー取りこぼし / .dds.zst 拡張子ルーティング）を特定・修正
* 既存 GUI バグも修正: gem コスト nil クラッシュ（self.costs→data.costs）、spell tooltip 白背景（無効ハンドル DrawImage を IsValid ガード）

* Synced upstream PathOfBuilding-PoE2 dev from Release 0.16.0 to 0.17.1 (23 commits)
* Calc/data layer (229 files): Zarokh's Revolt infinite DPS, Aggravated bleeding, Amazon crit, Trarthan Cannon crash, Rapid Casting III, 0.5 new data/runes/quest rewards, etc.
* Built a pipeline converting 0.17 tree art (.dds.zst BC7/BC1 texture arrays) into 2D atlas PNGs, since the macOS dylib's stb_image decodes PNG only
* Fixed tree-render crashes found in GUI testing (ddsCoords bare-identifier keys, .dds.zst extension routing)
* Fixed pre-existing GUI bugs: gem cost nil crash, spell tooltip white background

VERSION[0.8.0][2026/05/30]

--- phase9 sync 負債の修正 / phase9 sync debt fixes ---
* 上流 PathOfBuilding-PoE2 dev を Phase 6〜9 で同期（計算エンジン・データ・0.5 パッシブツリー・Compare/Loadout 機能）
* 起動3連クラッシュを解消（PassiveTreeView の GemTooltip/TradeHelpers 取込、checkUnlockConstraints/compareJewelsEqual 復元）
* repo-root の Assets/Data/TreeData 互換 symlink を復活（dylib のアーカイブ機構無効時の FS フォールバック解決）
* 0_5 ツリーアート（GGG プレリリース由来 webp）を PNG へ変換しノード/背景の描画を修復
* webp→PNG 変換を build パイプライン化（scripts/convert-tree-webp.sh + build-app.sh）で再 sync 後の再発を防止

VERSION[0.7.2][2026/05/03]

--- リポジトリ整理 / Repository Cleanup ---
* PathOfBuilding.app の中身をリポジトリ直下に展開（src/, runtime/, tree-data/, package/）
* .app は scripts/build-app.sh で組み立てる build artifact 化（dist/PathOfBuilding.app）
* dev mode (--dev): Lua ソースは symlink で配置、編集即反映
* release mode (--release): cp + codesign + zip 化
* SimpleGraphic / libSimpleGraphic / CharInput dylib + 起動ランチャーは tracked リソースとして runtime/ + package/MacOS/ に配置
* i18n 言語ファイルを言語別サブディレクトリ化（src/Locales/ja_*.lua → src/Locales/ja/*.lua）
* 重複ディレクトリ統合（doc/docs, skills/installed-skills）
* .gitignore の skills/ ルールが case-insensitive で src/Data/Skills/ も巻き込んでいた問題を修正
* BUILD.md 新設（初回 setup と build pipeline 説明）

--- バグ修正 / Bug Fixes ---
* PassiveTreeView.lua クラッシュ修正（Assets/Data compatibility symlink を build-app.sh で再生成）
* test ファイルの絶対パス参照を新レイアウトに更新（test_common, test_modtools, run_visual_test）

--- Repository Cleanup ---
* Extract PathOfBuilding.app contents to repo root (src/, runtime/, tree-data/, package/)
* The .app is now a build artifact assembled by scripts/build-app.sh into dist/
* Dev mode (--dev): symlink Lua sources for instant reload
* Release mode (--release): cp + codesign + zip
* SimpleGraphic / libSimpleGraphic / CharInput dylibs and the launcher script are tracked resources
* i18n locales reorganized into per-language subdirectories (src/Locales/ja_*.lua → src/Locales/ja/*.lua)
* Consolidate duplicate directories (doc/docs, skills/installed-skills)
* Fix .gitignore skills/ rule that was case-insensitively swallowing src/Data/Skills/
* New BUILD.md documenting initial setup and build pipeline

--- Bug Fixes ---
* Fix PassiveTreeView.lua crash by recreating Assets/Data compatibility symlinks in build-app.sh
* Update absolute path references in test files to the new layout

VERSION[0.7.1][2026/02/21]

--- 修正 / Fixes ---
* 最新PoE2ユニーク装備の翻訳漏れ修正（アイテム名9件追加）
* mod効果テンプレート12件追加（呪いスキルレベル、バラージ、クリティカルウィークネス、非チャネリングスペル等）
* Contents/Info.plistバージョン同期修正
* Fixed missing translations for latest PoE2 unique items (9 item names added)
* Added 12 mod stat line templates (curse skill levels, barrage, critical weakness, non-channelling spells, etc.)
* Fixed Contents/Info.plist version sync

VERSION[0.7.0][2026/02/21]

--- 新機能 / New Features ---
* パッシブツリー翻訳をi18nシステムに統合（TreeTranslations廃止）
* ja_passive_names.lua: 1911件のノード名翻訳を新規i18n補助ファイルとして抽出
* ja_mod_stat_lines.lua: 1077件の新規stat行テンプレートを追加
* 複数行stat結合ロジック追加（改行分割されたstat行を自動結合して翻訳）
* ノード名末尾空白トリム対応（5件8ノード）
* Attribute/Attributesノード名翻訳追加（296ノード）

--- バグ修正 / Bug Fixes ---
* 49件のハードコード数値をプレースホルダ形式に修正（翻訳テンプレート品質向上）
* "A maximum of one Modifer"の誤翻訳を修正
* multi-line stat結合時の消費行表示を修正（空行表示防止）

VERSION[0.6.0][2026/02/21]

--- 新機能 / New Features ---
* スキルジェムツールチップの日本語翻訳を大幅強化
* ジェム効果分岐ラベル（Explosion, Aftershock, Fire, Lightning, Cold等）約360件を翻訳辞書に追加
* gem_stat_descriptions全テンプレートを網羅チェックし、未翻訳145件を追加（statテンプレートカバレッジ100%達成）

--- バグ修正 / Bug Fixes ---
* ツールチップの改行時にカラーコードが消失する問題を修正（Tooltip.lua WrapString色引き継ぎ）
* calcFunc未取得時にジェムツールチップ全体が消失する問題を修正（GemSelectControl.lua）
* statSet.label / additional.name / gemFamilyの未翻訳表示を修正
* i18n.luaのデバッグログ出力を削除（translateModLine毎回のio.openによるパフォーマンス低下を解消）

VERSION[0.5.0][2026/02/20]

--- 新機能 / New Features ---
* 上流 PathOfBuilding-PoE2 dev ブランチから計算エンジン残り11モジュールを同期
* CalcSections: Spirit、Deflection、Freeze Buildup、Charm、Mark等のPoE2新stat表示追加
* BuildSiteTools: poe2db.tw対応、pob2:プロトコルハンドラ対応
* 日本語翻訳: CalcSections 72項目追加、アイテムMod行・ジェムstat・パッシブツリーのtranslateModLine対応

--- バグ修正 / Bug Fixes ---
* getGemStatRequirement引数順変更に伴うSkillsTab/GemSelectControlの呼び出し側修正
* StatDescriber: undefined floor修正、i18n stat description lookup復元

--- 同期ファイル / Synced Modules ---
* CalcSections/Calcs/CalcTools/BuildDisplayStats/CalcBreakdown/BuildSiteTools: 上流版に置換
* Data.lua/Common.lua/ModTools.lua/StatDescriber.lua/BuildList.lua: 手動マージ（ローカルパッチ保持）

VERSION[0.4.0][2026/02/20]

--- 新機能 / New Features ---
* 上流 PathOfBuilding-PoE2 dev ブランチから24データファイルを同期
* StatDescriptions 5ファイル: Mod説明文・スキルstat・ジェムstat・モンスターstat更新
* Bases 6ファイル: 剣・斧・メイス・ダガー・フレイル・スタッフの新ベース追加
* Uniques: 新ユニーク・レースユニーク追加
* TimelessJewel: レギオンパッシブ・ノードマッピング更新

--- バグ修正 / Bug Fixes ---
* ModTools.lua: 混合キー型ソートcomparator追加（Blackened Heart等の起動クラッシュ修正）
* Data.lua: metatableガード移設、PoE2武器タイプ・クラス対応

VERSION[0.3.0][2026/02/19]

--- 計算エンジン同期 / Calc Engine Sync ---
* 上流 PathOfBuilding-PoE2 dev ブランチから計算エンジンを同期（161コミット分のPoE2対応改善）
* CalcOffence: Firestorm/RoA等のDPS計算、ダメージ再帰構造を更新
* CalcDefence: PoE2のヒットチャンス/アーマー計算式、Deflection対応
* CalcPerform: Spirit計算、mergeKeystones対応
* CalcSetup: PoE2 characterConstants、statSets構造対応
* CalcActiveSkill: statSet構造、baseMods対応

--- バグ修正 / Bug Fixes ---
* ModParser: PoE2 Conquerors（Vorana等）、Mod解析改善
* ItemTools: formatValue復活、PoE2 influence対応
* CalcTriggers: Unleashable→CanRapidFire リネーム
* Data.lua: PoE2対応の定数・テーブル追加（DeflectEffect、buildupTypes、ailmentDamageTypes等）
* ソケットグループの日本語表示ラベル修正

VERSION[0.2.2][2026/02/19]

--- 新機能 / New Features ---
* SGPAKアーカイブからのアセット読み込みシステムを追加
* アーカイブのシャットダウン時クリーンアップ（ハンドルリーク防止）

--- バグ修正 / Bug Fixes ---
* FFI nullポインタチェックの強化
* アーカイブ読み込み失敗時の警告ログ追加（サイレント失敗の防止）
* 未使用のMemory Budget API宣言を削除

VERSION[0.2.1][2026/02/18]

--- セキュリティ修正 / Security Fixes ---
* OAuth PKCE認証の暗号強度を改善（math.random → /dev/urandom、256bit エントロピー）
* TradeQuery通貨ファイルのパストラバーサル脆弱性を修正
* 外部ビルドリストのascendancy名によるパストラバーサルを修正
* ファイルパス検証の改行文字バイパスを修正

--- バグ修正 / Bug Fixes ---
* base64urlパディング処理の修正
* io.open nilガード追加（通貨ファイル書き込み時のクラッシュ防止）

VERSION[0.2.0][2026/02/17]

--- 日本語ローカライゼーション / Japanese Localization ---
* 日本語ローカライゼーション対応（UI全タブ、パッシブツリー、アイテム、スキル、Mod）
* パッシブツリーノード名 589件の日本語翻訳
* ユニークアイテム名・フレーバーテキスト 374件の日本語翻訳
* ジェム説明文・Mod統計行 3,298件の日本語翻訳

--- 安定性改善 / Stability ---
* SubScriptライフサイクル安全性の向上（コールバッククラッシュ防止）
* 大規模テーブル走査のyield改善（フレームスタッター軽減）
* ImageHandle安全性の向上（ダングリングポインタ修正）

--- テスト / Testing ---
* Bustedユニットテスト基盤（71テスト）+ GitHub Actions CI
* ビジュアルリグレッションテスト基盤

VERSION[0.1.2][2026/02/11]

--- 変更 / Changes ---
* ConfigOptionsをPoE2上流版に差し替え（Quest Rewards追加、Bandit/Pantheon削除）
* Warningsポップアップの表示順序修正（他のウィジェットの上に表示）
* 旧セーブデータ互換性のためのnil安全性修正

VERSION[0.1.1][2026/02/11]

--- バグ修正 / Bug Fixes ---
* ビルドのセーブ/ロード機能を有効化（macOSパス設定修正）

VERSION[0.1.0][2026/02/11]

--- 初回リリース / Initial Release ---
* macOS ネイティブ動作（Metal / ARM64）
* パッシブツリーの表示・ノード割り当て・ステータス反映
* ビルド画面の全7タブ（Tree, Skills, Items, Calcs, Config, Notes, Import/Export）
* PoB リンクからのビルドインポート
* リアルタイムステータス再計算
* ツールチップ表示（アイテム・ノード・スキル）
* PoE2 武器タイプ対応（Spear, Flail, Crossbow, Talisman）
