{
  "name": "logos-orbit",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/shared",
    "packages/integrity",
    "packages/chronos",
    "packages/nomos"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared && npm run build -w packages/integrity && npm run build -w packages/chronos && npm run build -w packages/nomos",
    "clean": "rm -rf packages/*/dist",
    "test": "npm test --workspaces --if-present"
  },
  "engines": { "node": ">=18" }
}
