.DS_Store
.claude/

# Local docs and skills (not part of the app distribution)
doc/
skills/
.worktrees/

# Dev tools and session artifacts
tools/
.mcp.json
RESEARCH*.md
test.md
test_results_*.md

# Build output (assembled by scripts/build-app.sh)
dist/

# C++ build artifacts
pob2macos/
pob2macos/simplegraphic/build/

# TimelessJewel data: tracked despite *.zip rule (upstream data assets)
*.zip
!src/Data/TimelessJewelData/*.zip*

# Large binary assets (distribute via Releases)
*.dds.zst
tree-data/**/*.png
tree-data/**/*.jpg

# Standard fonts (install separately, not bundled in repo)
runtime/fonts/LiberationSans-*.ttf

# Local working files and temp artifacts
tmp/
*.lua.new
*.lua.bak
*.dylib.bak*
*.log
