#!/bin/zsh
set -euo pipefail
cd "$(dirname "$0")/../Resources"
LOG_DIR="/Users/kokage/national-operations/pob2macos/debug"
mkdir -p "$LOG_DIR"
LOG="$LOG_DIR/passive_tree_app.log"
echo "=== RUN $(date) ===" >> "$LOG"
exec /usr/local/bin/luajit "./pob2_launch.lua" >> "$LOG" 2>&1
